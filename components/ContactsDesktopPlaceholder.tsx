export function ContactsDesktopPlaceholder() {
  return (
    <section className="screen contacts-desktop-empty">
      <p className="contacts-empty-lede">Select someone from the list</p>
      <p className="empty">
        Pick a person to see their timeline, or use Capture to log a new note.
      </p>
    </section>
  );
}
